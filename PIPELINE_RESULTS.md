# Результаты после завершения pipeline

После успешного выполнения `python run_full_pipeline.py` будут созданы следующие результаты:

## 📁 Структура созданных файлов и папок

```
hahaton123/
├── 📁 config/
│   └── ppe_data.yaml                    # ✅ Конфигурация датасета (создается автоматически)
│
├── 📁 data/
│   ├── 📁 images/
│   │   ├── 📁 train/                   # ✅ Изображения для обучения (80%)
│   │   │   ├── video1_frame_000000.jpg
│   │   │   ├── video1_frame_000015.jpg
│   │   │   └── ...
│   │   └── 📁 val/                      # ✅ Изображения для валидации (20%)
│   │       ├── video1_frame_000100.jpg
│   │       └── ...
│   └── 📁 labels/
│       ├── 📁 train/                    # ✅ Разметка для обучения
│       │   ├── video1_frame_000000.txt
│       │   └── ...
│       └── 📁 val/                       # ✅ Разметка для валидации
│           └── ...
│
├── 📁 models/
│   └── 📁 ppe_detection/                # ✅ Директория эксперимента
│       ├── 📁 weights/
│       │   ├── best.pt                   # ⭐ ЛУЧШАЯ МОДЕЛЬ (используйте эту!)
│       │   └── last.pt                   # Последняя модель
│       ├── 📁 train_batch*.jpg           # Примеры батчей во время обучения
│       ├── 📁 val_batch*.jpg             # Примеры валидационных батчей
│       ├── results.png                   # 📊 График метрик обучения
│       ├── results.csv                   # 📊 Детальные метрики по эпохам
│       ├── confusion_matrix.png          # 📊 Матрица ошибок
│       ├── F1_curve.png                  # 📊 F1 кривая
│       ├── PR_curve.png                  # 📊 Precision-Recall кривая
│       ├── args.yaml                     # Параметры обучения
│       └── events.out.tfevents.*        # TensorBoard логи (если включен)
│
├── 📁 output/
│   ├── 📁 detections/                   # ✅ Результаты тестовой детекции
│   │   └── [имя_тестового_изображения].jpg  # Изображение с нарисованными bounding boxes
│   └── 📁 videos/                        # (если обрабатывали видео)
│
├── 📁 logs/
│   └── pipeline_YYYYMMDD_HHMMSS.log      # ✅ Полный лог выполнения pipeline
│
└── 📁 models/logs/                      # Логи обучения
    └── ppe_detection_YYYYMMDD_HHMMSS.log
```

## ⭐ Главные результаты

### 1. **Обученная модель** (самое важное!)

**Путь:** `models/ppe_detection/weights/best.pt`

- Это лучшая модель по метрике mAP50
- Используйте её для детекции на новых изображениях
- Размер: обычно 50-200 MB (зависит от модели)

**Альтернатива:** `models/ppe_detection/weights/last.pt`
- Последняя модель после всех эпох
- Может быть хуже чем best.pt

### 2. **Метрики обучения**

**Файл:** `models/ppe_detection/results.csv`

Содержит метрики по каждой эпохе:
- `metrics/mAP50(B)` - средняя точность при IoU=0.5
- `metrics/mAP50-95(B)` - средняя точность при IoU=0.5-0.95
- `metrics/precision(B)` - точность
- `metrics/recall(B)` - полнота
- `train/box_loss` - loss для bounding boxes
- `train/cls_loss` - loss для классификации
- И другие метрики

**Графики:** `models/ppe_detection/results.png`
- Визуализация всех метрик в виде графиков
- Показывает прогресс обучения

### 3. **Тестовая детекция**

**Папка:** `output/detections/`

- Пример детекции на одном изображении из валидации
- Показывает, как модель работает на реальных данных
- Изображение с нарисованными bounding boxes

## 📊 Как проверить качество модели

### 1. Посмотреть метрики в CSV

```python
import pandas as pd

df = pd.read_csv("models/ppe_detection/results.csv")
print(df[['metrics/mAP50(B)', 'metrics/mAP50-95(B)']].tail())
```

**Хорошие значения:**
- mAP50 > 0.7 (70%) - отлично
- mAP50 > 0.5 (50%) - хорошо
- mAP50 > 0.3 (30%) - приемлемо для начала

### 2. Посмотреть графики

Откройте `models/ppe_detection/results.png` - там будут графики:
- Метрики должны расти (или стабилизироваться)
- Loss должен уменьшаться

### 3. Протестировать на своих изображениях

```python
from src.inference.detect_utils import PPEDetector

# Загружаем лучшую модель
detector = PPEDetector("models/ppe_detection/weights/best.pt")

# Тестируем на изображении
result_img, detections = detector.detect_image(
    "путь/к/изображению.jpg",
    save_result=True
)

print(f"Найдено детекций: {len(detections)}")
for det in detections:
    print(f"  {det['class_name']}: {det['confidence']:.2f}")
```

## 🎯 Что можно делать дальше

### 1. Использовать модель для детекции

**Через ноутбук (рекомендуется):**
```bash
jupyter lab notebooks/inference.ipynb
```

**Через Python:**
```python
from src.inference.detect_utils import PPEDetector

detector = PPEDetector("models/ppe_detection/weights/best.pt")

# На изображении
result_img, detections = detector.detect_image("image.jpg", save_result=True)

# На видео
detector.detect_video("video.mp4", output_path="output/detected.mp4")

# С камеры (реальное время)
detector.detect_camera(camera_id=0)
```

### 2. Улучшить модель (если нужно)

Если метрики низкие:
- Добавьте больше данных
- Проверьте качество разметки
- Увеличьте количество эпох
- Попробуйте другую модель (yolov8m.pt, yolov8x.pt)

### 3. Визуализировать разметку

```bash
# Проверить разметку на изображениях
python visualize_labels.py

# Только валидационные изображения
python visualize_labels.py --split val

# Ограничить количество
python visualize_labels.py --limit 20
```

## 📈 Типичные результаты

После 30 эпох обучения с оптимизированными параметрами:

**Ожидаемые метрики:**
- mAP50: 0.5 - 0.8 (зависит от качества данных)
- mAP50-95: 0.3 - 0.6
- Время обучения: 1-2 часа (на GPU)

**Если метрики низкие (< 0.3):**
- Проверьте качество разметки
- Убедитесь, что достаточно данных (минимум 100-200 изображений на класс)
- Попробуйте увеличить количество эпох до 50-100

## 🔍 Проверка результатов

### Быстрая проверка:

1. **Модель существует?**
   ```bash
   ls models/ppe_detection/weights/best.pt
   ```

2. **Метрики есть?**
   ```bash
   ls models/ppe_detection/results.csv
   ```

3. **Графики созданы?**
   ```bash
   ls models/ppe_detection/results.png
   ```

4. **Тестовая детекция работает?**
   ```bash
   ls output/detections/
   ```

## ⚠️ Важные файлы

**Обязательно сохраните:**
- ✅ `models/ppe_detection/weights/best.pt` - лучшая модель
- ✅ `models/ppe_detection/results.csv` - метрики
- ✅ `config/ppe_data.yaml` - конфигурация датасета

**Можно удалить (для экономии места):**
- `models/ppe_detection/weights/last.pt` - если best.pt достаточно
- `models/ppe_detection/train_batch*.jpg` - примеры батчей
- `logs/` - старые логи (если не нужны)

## 📝 Логи

Все действия логируются в:
- `logs/pipeline_YYYYMMDD_HHMMSS.log` - полный лог pipeline
- `models/logs/ppe_detection_YYYYMMDD_HHMMSS.log` - лог обучения

В логах можно найти:
- Все выполненные этапы
- Метрики обучения
- Ошибки (если были)
- Время выполнения каждого этапа

